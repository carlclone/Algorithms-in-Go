func multiply(num1 string, num2 string) string {
    //生成一个 m+n 长度,默认值为 0 的数组 因为结果最多为 m+n位
    
    //从个位数开始每位相乘 , 然后把结果叠加放置在正确的位置上
    
    //需要把字符(字节)转成数字类型
    
    //跳过前面未使用的位 0
    
    //将数字转回字节(字符) , 字节数组 (字符串)
    
    //如果数组大小为 0 , 则输出 0 的字节数组(字符串)
}
